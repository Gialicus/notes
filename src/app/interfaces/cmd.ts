export enum Cmd {
  ADD_NOTE = "add_note",
  BROWSE_NOTE = "browse_note",
  EDIT_NOTE = "edit_note",
  READ_NOTE = "read_note",
  DELETE_NOTE = "delete_note",
}

export enum RustEvent {
  MENU_SELECTED = "menu-selected",
}
